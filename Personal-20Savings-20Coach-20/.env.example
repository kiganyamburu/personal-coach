# Server Environment Variables (Backend)
# These are used by the Express server and should NEVER be exposed to the client

# Firebase Admin SDK Configuration
# Get this from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}

# Google Cloud / Vertex AI Configuration
GOOGLE_CLOUD_PROJECT_ID=your-project-id
VERTEX_AI_LOCATION=us-central1

# Google Gemini API Key (Simpler alternative to Vertex AI)
# Get from: https://makersuite.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=your-gemini-api-key

# Optional: Other server configurations
PING_MESSAGE="ping pong"
